import{posteriorProbability}from"../logic.mjs";import{createTaskSession,finishSingleQuestion,wireCommonButtons}from"../shared.mjs";const options=[10,31,80,90];let order;let startedAt;function start(){startedAt=performance.now();order=[...options].sort(()=>Math.random()-.5);document.querySelector("#choices").innerHTML=order.map(value=>`<label><input type="radio" name="answer" value="${value}"> 約${value}%</label>`).join("");}wireCommonButtons(start);document.querySelector("#answer-form").addEventListener("submit",event=>{event.preventDefault();const answer=Number(new FormData(event.currentTarget).get("answer"));if(!answer){document.querySelector(".question-error").textContent="確率を1つ選んでください。";return;}const posterior=posteriorProbability({baseTarget:.1,sensitivity:.8,falsePositiveRate:.2});const correct=answer===31;const row={...createTaskSession("base-rate"),base_rate_b:.1,sensitivity:.8,false_positive_rate:.2,response_percent:answer,normative_percent:Number((posterior*100).toFixed(3)),correctness:correct?1:0,option_order:order.join("|"),rt:Number((performance.now()-startedAt).toFixed(3))};finishSingleQuestion({taskId:"base-rate",row,summaryHtml:`<strong>${correct?"Bayesの定理による値に最も近い回答です":"基準率を含めて計算してみましょう"}</strong><p>規範的な事後確率：約30.8%</p>`,explanationHtml:`<h2>1000個で考える</h2><div class="probability-work"><table><thead><tr><th></th><th>製品数</th><th>「Z」と表示</th></tr></thead><tbody><tr><th>工場B</th><td>100</td><td>80</td></tr><tr><th>工場A</th><td>900</td><td>180</td></tr></tbody></table></div><p>「Z」は合計260個で、そのうちB製は80個です。したがって 80 ÷ 260 ≈ 30.8% です。80%はB製品にZが出る確率であり、Zが出たときB製である逆向きの確率とは異なります。</p>`});});
