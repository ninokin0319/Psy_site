import{chooseFrame,framingExpectedOutcomes}from"../logic.mjs";import{createTaskSession,finishSingleQuestion,wireCommonButtons}from"../shared.mjs";let frame;let startedAt;const text={gain:[{value:"sure",label:"計画A：200本が助かる。"},{value:"risky",label:"計画B：1/3の確率で600本すべてが助かり、2/3の確率で1本も助からない。"}],loss:[{value:"sure",label:"計画A：400本が失われる。"},{value:"risky",label:"計画B：1/3の確率で1本も失われず、2/3の確率で600本すべてが失われる。"}]};function start(){frame=chooseFrame();startedAt=performance.now();document.querySelector("#frame-options").innerHTML=text[frame].map(option=>`<label><input type="radio" name="answer" value="${option.value}"> ${option.label}</label>`).join("");}wireCommonButtons(start);document.querySelector("#answer-form").addEventListener("submit",event=>{event.preventDefault();const answer=new FormData(event.currentTarget).get("answer");if(!answer){document.querySelector(".question-error").textContent="計画を1つ選んでください。";return;}const expected=framingExpectedOutcomes();const row={...createTaskSession("framing"),frame_condition:frame,response:answer,expected_saved_a:expected.optionAExpectedSaved,expected_saved_b:expected.optionBExpectedSaved,expected_lost_a:expected.optionAExpectedLost,expected_lost_b:expected.optionBExpectedLost,rt:Number((performance.now()-startedAt).toFixed(3))};finishSingleQuestion({taskId:"framing",row,summaryHtml:`<strong>${answer==="sure"?"確実な計画A":"確率的な計画B"}を選びました</strong><p>提示条件：${frame==="gain"?"利得（助かる）":"損失（失われる）"}フレーム</p>`,explanationHtml:`<h2>2つの表現は対応しています</h2><p>計画Aは「200本が助かる」と「400本が失われる」が同じ結果です。計画Bも「1/3で全て助かる」と「1/3で何も失われない」が対応します。期待される本数はどちらの計画も200本が助かり、400本が失われます。</p><p>ただし、計画AとBは結果のばらつきが異なるため、どちらを選ぶかに単一の正解を付けません。フレーミング効果は利得条件と損失条件の選択率差として調べます。</p>`});});
